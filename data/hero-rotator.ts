/**
 * Phrases that rotate in the hero headline.
 * Keep lengths similar so the layout stays stable.
 */
export const heroRotatorPhrases = [
  "engineered intent.",
  "deliberate craft.",
  "secure systems.",
  "precise interfaces.",
  "curious engineering.",
] as const;
